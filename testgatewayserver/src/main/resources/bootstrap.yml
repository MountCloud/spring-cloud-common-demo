spring:
   application:
      name: demo-gateway-server
   profiles:
      active: dev
   jackson:
      time-zone: GMT+8
      date-format: yyyy-MM-dd HH:mm:ss
   main:
      allow-bean-definition-overriding: true
server:
   port: 8904
   context-path: /