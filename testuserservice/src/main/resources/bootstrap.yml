spring:
   application:
      name: demo-user-service
   profiles:
      active: dev
   jackson:
      time-zone: GMT+8
      date-format: yyyy-MM-dd HH:mm:ss
   main:
      allow-bean-definition-overriding: true
server:
   port: 8803
   context-path: /

ribbon:
   ConnectTimeout: 5000
   ReadTimeout: 30000